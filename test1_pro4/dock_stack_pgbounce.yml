---
 - hosts: managers
   remote_user: ubuntu
   become: true
   gather_facts: False
   tasks:
    - pip:
       name: jsondiff
       state: present

    - pip:
       name: pyyaml
       state: present

    - name: deploying pgbouncer in the docker stack with compose file 
      docker_stack:
       state: present
       name: pgbouncer
       compose:
        - docker-compose.yml 

 
